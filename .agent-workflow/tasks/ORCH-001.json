{
  "id": "ORCH-003D1B0",
  "status": "APPROVED",
  "parentEpic": "ORCHESTRATION-PILOT",
  "objective": "Prove that a real rootless non-root worker receives only one direct task workspace mount",
  "nonGoals": ["Run Codex", "Create Git branches or worktrees", "Mount a shared workspace root", "Delete retained workspaces", "Change container policy"],
  "dependsOn": ["ORCH-003D1A0A1A0"],
  "acceptanceCriteria": [
    "direct-child-mount|WHEN the worker command is constructed|ASSERT TEST_PASS workspace-isolation-tests",
    "rootless-worker|WHEN the real local containment probe runs|ASSERT TEST_PASS workspace-rootless-proof",
    "sibling-denial|WHEN the worker checks root and sibling paths and descriptors|ASSERT TEST_PASS workspace-rootless-proof",
    "bounded-output|WHEN the Podman commands execute|ASSERT TEST_PASS workspace-isolation-tests"
  ],
  "acceptanceEvidenceIds": {
    "direct-child-mount|WHEN the worker command is constructed|ASSERT TEST_PASS workspace-isolation-tests": ["workspace-isolation-tests"],
    "rootless-worker|WHEN the real local containment probe runs|ASSERT TEST_PASS workspace-rootless-proof": ["workspace-rootless-proof"],
    "sibling-denial|WHEN the worker checks root and sibling paths and descriptors|ASSERT TEST_PASS workspace-rootless-proof": ["workspace-rootless-proof"],
    "bounded-output|WHEN the Podman commands execute|ASSERT TEST_PASS workspace-isolation-tests": ["workspace-isolation-tests"]
  },
  "acceptanceEvidenceRequirements": {},
  "requiredTests": [".agent-workflow/scripts/verify_controller.sh", "python3 .agent-workflow/scripts/check_repository.py"],
  "allowedPaths": [
    ".agent-workflow/controller/td_controller/workspace_isolation.py",
    ".agent-workflow/controller/tests/test_workspace_isolation.py",
    ".agent-workflow/tasks/ORCH-001.json"
  ],
  "protectedPaths": [".github/**"],
  "riskClass": "R3",
  "maxChangedLines": 450,
  "maxAttempts": 2,
  "humanApprovalRequired": true
}

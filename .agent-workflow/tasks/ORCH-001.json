{
  "id": "ORCH-003D1B0N4",
  "status": "APPROVED",
  "parentEpic": "ORCHESTRATION-PILOT",
  "objective": "Compose pinned repository cwd and verified workspace-descriptor substitution in one bounded trusted call",
  "nonGoals": ["Require a live workspace handle", "Observe worktree state", "Run against the project repository", "Checkout files", "Commit or push"],
  "dependsOn": ["ORCH-003D1B0L6", "ORCH-003D1B0N3"],
  "acceptanceCriteria": [
    "dual-descriptor|WHEN an exact disposable command runs|ASSERT TEST_PASS pinned-workspace-argument-tests",
    "repository-cwd|WHEN Git resolves its repository|ASSERT VALUE \"pinned repository\"",
    "workspace-substitution|WHEN workspace pathname is replaced|ASSERT ABSENT replacement-git-marker",
    "identity-mismatch|WHEN workspace evidence differs|ASSERT ERROR PinnedDirectoryExecutorError",
    "diagnostic-boundary|WHEN descriptor substitution fails|ASSERT ABSENT raw-cause"
  ],
  "acceptanceEvidenceIds": {
    "dual-descriptor|WHEN an exact disposable command runs|ASSERT TEST_PASS pinned-workspace-argument-tests": ["pinned-workspace-argument-tests"],
    "repository-cwd|WHEN Git resolves its repository|ASSERT VALUE \"pinned repository\"": ["pinned-workspace-argument-tests"],
    "workspace-substitution|WHEN workspace pathname is replaced|ASSERT ABSENT replacement-git-marker": ["pinned-workspace-argument-tests"],
    "identity-mismatch|WHEN workspace evidence differs|ASSERT ERROR PinnedDirectoryExecutorError": ["pinned-workspace-argument-tests"],
    "diagnostic-boundary|WHEN descriptor substitution fails|ASSERT ABSENT raw-cause": ["pinned-workspace-argument-tests"]
  },
  "acceptanceEvidenceRequirements": {},
  "requiredTests": [".agent-workflow/scripts/verify_controller.sh", "python3 .agent-workflow/scripts/check_repository.py"],
  "allowedPaths": [
    ".agent-workflow/controller/td_controller/pinned_directory_executor.py",
    ".agent-workflow/controller/tests/test_pinned_workspace_argument.py",
    ".agent-workflow/tasks/ORCH-001.json"
  ],
  "protectedPaths": [".github/**"],
  "riskClass": "R3",
  "maxChangedLines": 350,
  "maxAttempts": 2,
  "humanApprovalRequired": true
}

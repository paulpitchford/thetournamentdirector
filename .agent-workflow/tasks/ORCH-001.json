{
  "id": "ORCH-003D1A0",
  "status": "APPROVED",
  "parentEpic": "ORCHESTRATION-PILOT",
  "objective": "Pin trusted pre-provisioned workspace descriptors internally without pathname access or descriptor export",
  "nonGoals": ["Create workspace directories", "Export a workspace descriptor", "Provide task-lease exclusivity", "Delete or rename workspace directories", "Run Git commands"],
  "dependsOn": ["ORCH-003C"],
  "acceptanceCriteria": [
    "preprovisioned-pin|WHEN a trusted workspace descriptor is registered|ASSERT TEST_PASS workspace-storage-tests",
    "path-independent|WHEN the workspace pathname is replaced|ASSERT TEST_PASS workspace-storage-tests",
    "no-path-access|WHEN registration and inspection run|ASSERT TEST_PASS workspace-storage-tests",
    "fail-closed|WHEN permissions, identity, or descriptor type changes|ASSERT TEST_PASS workspace-storage-tests"
  ],
  "acceptanceEvidenceIds": {
    "preprovisioned-pin|WHEN a trusted workspace descriptor is registered|ASSERT TEST_PASS workspace-storage-tests": ["workspace-storage-tests"],
    "path-independent|WHEN the workspace pathname is replaced|ASSERT TEST_PASS workspace-storage-tests": ["workspace-storage-tests"],
    "no-path-access|WHEN registration and inspection run|ASSERT TEST_PASS workspace-storage-tests": ["workspace-storage-tests"],
    "fail-closed|WHEN permissions, identity, or descriptor type changes|ASSERT TEST_PASS workspace-storage-tests": ["workspace-storage-tests"]
  },
  "acceptanceEvidenceRequirements": {},
  "requiredTests": [".agent-workflow/scripts/verify_controller.sh", "python3 .agent-workflow/scripts/check_repository.py"],
  "allowedPaths": [
    ".agent-workflow/controller/td_controller/workspace_storage.py",
    ".agent-workflow/controller/tests/test_workspace_storage.py",
    ".agent-workflow/tasks/ORCH-001.json"
  ],
  "protectedPaths": [".github/**"],
  "riskClass": "R3",
  "maxChangedLines": 400,
  "maxAttempts": 2,
  "humanApprovalRequired": true
}

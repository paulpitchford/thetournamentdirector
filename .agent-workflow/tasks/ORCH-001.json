{
  "id": "ORCH-003D1B0O3B",
  "status": "APPROVED",
  "parentEpic": "ORCHESTRATION-PILOT",
  "objective": "Read exactly bounded descriptor bytes while correctly accumulating legal short reads",
  "nonGoals": ["Open a pathname", "Validate descriptor identity", "Write workspace files", "Commit or push"],
  "dependsOn": ["ORCH-003D1B0O2"],
  "acceptanceCriteria": [
    "exact-read|WHEN real binary descriptor has expected bytes|ASSERT TEST_PASS bounded-fd-read-tests",
    "short-read|WHEN multiple legal short reads occur|ASSERT VALUE \"exact accumulated payload\"",
    "short-eof|WHEN EOF occurs before expected size|ASSERT ERROR BoundedFdReadError",
    "long-content|WHEN content exceeds expected size|ASSERT ERROR BoundedFdReadError",
    "invalid-bound|WHEN descriptor or size is invalid|ASSERT ERROR BoundedFdReadError"
  ],
  "acceptanceEvidenceIds": {
    "exact-read|WHEN real binary descriptor has expected bytes|ASSERT TEST_PASS bounded-fd-read-tests": ["bounded-fd-read-tests"],
    "short-read|WHEN multiple legal short reads occur|ASSERT VALUE \"exact accumulated payload\"": ["bounded-fd-read-tests"],
    "short-eof|WHEN EOF occurs before expected size|ASSERT ERROR BoundedFdReadError": ["bounded-fd-read-tests"],
    "long-content|WHEN content exceeds expected size|ASSERT ERROR BoundedFdReadError": ["bounded-fd-read-tests"],
    "invalid-bound|WHEN descriptor or size is invalid|ASSERT ERROR BoundedFdReadError": ["bounded-fd-read-tests"]
  },
  "acceptanceEvidenceRequirements": {},
  "requiredTests": [".agent-workflow/scripts/verify_controller.sh", "python3 .agent-workflow/scripts/check_repository.py"],
  "allowedPaths": [
    ".agent-workflow/controller/td_controller/bounded_fd_read.py",
    ".agent-workflow/controller/tests/test_bounded_fd_read.py",
    ".agent-workflow/tasks/ORCH-001.json"
  ],
  "protectedPaths": [".github/**"],
  "riskClass": "R3",
  "maxChangedLines": 250,
  "maxAttempts": 2,
  "humanApprovalRequired": true
}

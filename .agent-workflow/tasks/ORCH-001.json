{
  "id": "ORCH-003D1B0L6",
  "status": "APPROVED",
  "parentEpic": "ORCHESTRATION-PILOT",
  "objective": "Serialize a trusted multi-command operation across one pinned directory descriptor lifetime",
  "nonGoals": ["Expose a pathname", "Expose a descriptor", "Define Git commands", "Run Git mutation", "Create a worktree"],
  "dependsOn": ["ORCH-003D1B0L5"],
  "acceptanceCriteria": [
    "multi-command|WHEN two trusted commands run in one hold|ASSERT TEST_PASS pinned-execution-hold-tests",
    "close-serialization|WHEN close overlaps a held operation|ASSERT VALUE \"close waits\"",
    "nested-hold|WHEN the same thread nests a hold|ASSERT ERROR PinnedDirectoryExecutorError",
    "post-verification|WHEN held code fails and directory mode changes|ASSERT VALUE \"executor poisoned\""
  ],
  "acceptanceEvidenceIds": {
    "multi-command|WHEN two trusted commands run in one hold|ASSERT TEST_PASS pinned-execution-hold-tests": ["pinned-execution-hold-tests"],
    "close-serialization|WHEN close overlaps a held operation|ASSERT VALUE \"close waits\"": ["pinned-execution-hold-tests"],
    "nested-hold|WHEN the same thread nests a hold|ASSERT ERROR PinnedDirectoryExecutorError": ["pinned-execution-hold-tests"],
    "post-verification|WHEN held code fails and directory mode changes|ASSERT VALUE \"executor poisoned\"": ["pinned-execution-hold-tests"]
  },
  "acceptanceEvidenceRequirements": {},
  "requiredTests": [".agent-workflow/scripts/verify_controller.sh", "python3 .agent-workflow/scripts/check_repository.py"],
  "allowedPaths": [
    ".agent-workflow/controller/td_controller/pinned_directory_executor.py",
    ".agent-workflow/controller/tests/test_pinned_execution_hold.py",
    ".agent-workflow/tasks/ORCH-001.json"
  ],
  "protectedPaths": [".github/**"],
  "riskClass": "R3",
  "maxChangedLines": 250,
  "maxAttempts": 2,
  "humanApprovalRequired": true
}

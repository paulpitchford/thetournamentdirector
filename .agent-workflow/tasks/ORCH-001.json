{
  "id": "ORCH-003D1B0L",
  "status": "APPROVED",
  "parentEpic": "ORCHESTRATION-PILOT",
  "objective": "Pin one canonical controller repository pathname to a live owned directory descriptor",
  "nonGoals": ["Inspect Git metadata", "Reserve a branch", "Create a worktree", "Relate repository and task directories", "Mutate filesystem names"],
  "dependsOn": ["ORCH-003D1B0K3", "ORCH-003D1B0K5"],
  "acceptanceCriteria": [
    "descriptor-binding|WHEN repository authority is created|ASSERT TEST_PASS repository-identity-handle-tests",
    "path-replacement|WHEN the canonical root name is replaced|ASSERT ERROR RepositoryIdentityHandleError",
    "live-hold|WHEN close overlaps a held operation|ASSERT VALUE \"serialized\"",
    "unsafe-root|WHEN path descriptor ownership or mode is unsafe|ASSERT ERROR RepositoryIdentityHandleError"
  ],
  "acceptanceEvidenceIds": {
    "descriptor-binding|WHEN repository authority is created|ASSERT TEST_PASS repository-identity-handle-tests": ["repository-identity-handle-tests"],
    "path-replacement|WHEN the canonical root name is replaced|ASSERT ERROR RepositoryIdentityHandleError": ["repository-identity-handle-tests"],
    "live-hold|WHEN close overlaps a held operation|ASSERT VALUE \"serialized\"": ["repository-identity-handle-tests"],
    "unsafe-root|WHEN path descriptor ownership or mode is unsafe|ASSERT ERROR RepositoryIdentityHandleError": ["repository-identity-handle-tests"]
  },
  "acceptanceEvidenceRequirements": {},
  "requiredTests": [".agent-workflow/scripts/verify_controller.sh", "python3 .agent-workflow/scripts/check_repository.py"],
  "allowedPaths": [
    ".agent-workflow/controller/td_controller/repository_identity_handle.py",
    ".agent-workflow/controller/tests/test_repository_identity_handle.py",
    ".agent-workflow/tasks/ORCH-001.json"
  ],
  "protectedPaths": [".github/**"],
  "riskClass": "R3",
  "maxChangedLines": 450,
  "maxAttempts": 2,
  "humanApprovalRequired": true
}

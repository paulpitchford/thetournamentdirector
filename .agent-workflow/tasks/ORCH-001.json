{
  "id": "ORCH-003D1B0N1",
  "status": "APPROVED",
  "parentEpic": "ORCHESTRATION-PILOT",
  "objective": "Expose immutable non-authoritative pinned-directory identity evidence for later capability composition",
  "nonGoals": ["Expose a pathname", "Expose a descriptor", "Authorize an operation from identity evidence", "Run Git", "Create a worktree"],
  "dependsOn": ["ORCH-003D1B0L6"],
  "acceptanceCriteria": [
    "frozen-identity|WHEN pinned directory identity is read|ASSERT TEST_PASS pinned-directory-identity-tests",
    "no-authority|WHEN identity fields are inspected|ASSERT ABSENT descriptor",
    "closed-capability|WHEN identity survives close|ASSERT ERROR PinnedDirectoryExecutorError",
    "substitution-evidence|WHEN two directories are pinned|ASSERT VALUE \"identities differ\""
  ],
  "acceptanceEvidenceIds": {
    "frozen-identity|WHEN pinned directory identity is read|ASSERT TEST_PASS pinned-directory-identity-tests": ["pinned-directory-identity-tests"],
    "no-authority|WHEN identity fields are inspected|ASSERT ABSENT descriptor": ["pinned-directory-identity-tests"],
    "closed-capability|WHEN identity survives close|ASSERT ERROR PinnedDirectoryExecutorError": ["pinned-directory-identity-tests"],
    "substitution-evidence|WHEN two directories are pinned|ASSERT VALUE \"identities differ\"": ["pinned-directory-identity-tests"]
  },
  "acceptanceEvidenceRequirements": {},
  "requiredTests": [".agent-workflow/scripts/verify_controller.sh", "python3 .agent-workflow/scripts/check_repository.py"],
  "allowedPaths": [
    ".agent-workflow/controller/td_controller/pinned_directory_executor.py",
    ".agent-workflow/controller/tests/test_pinned_directory_identity.py",
    ".agent-workflow/tasks/ORCH-001.json"
  ],
  "protectedPaths": [".github/**"],
  "riskClass": "R3",
  "maxChangedLines": 250,
  "maxAttempts": 2,
  "humanApprovalRequired": true
}

{
  "id": "ORCH-002A",
  "status": "APPROVED",
  "parentEpic": "ORCHESTRATION-PILOT",
  "objective": "Add a strict durable task contract before read-only planning trials",
  "nonGoals": [
    "Implement planner output or dependency-graph validation",
    "Dispatch implementation or planning agents",
    "Change GitHub workflows, pilot policy, or merge controls"
  ],
  "dependsOn": ["ORCH-001D"],
  "acceptanceCriteria": [
    "Task JSON rejects duplicate keys recursively, unknown or missing fields, invalid primitive types, and oversized files",
    "Task identity, state, risk, numeric limits, path boundaries, dependencies, and human-approval fields are validated without coercion",
    "Acceptance criteria reject empty, duplicate, and deterministically vague statements",
    "Criterion evidence mappings reject unknown criteria and unapproved trusted evidence sources",
    "Ignored recovered roots, absolute paths, parent traversal, and exact allowed/protected overlap are rejected",
    "Existing repository task fixtures validate successfully",
    "Automated contract tests invoke no model"
  ],
  "acceptanceEvidenceIds": {
    "Task JSON rejects duplicate keys recursively, unknown or missing fields, invalid primitive types, and oversized files": ["local-task-contract-tests"],
    "Task identity, state, risk, numeric limits, path boundaries, dependencies, and human-approval fields are validated without coercion": ["local-task-contract-tests"],
    "Acceptance criteria reject empty, duplicate, and deterministically vague statements": ["local-task-contract-tests"],
    "Criterion evidence mappings reject unknown criteria and unapproved trusted evidence sources": ["local-task-contract-tests"],
    "Ignored recovered roots, absolute paths, parent traversal, and exact allowed/protected overlap are rejected": ["local-task-contract-tests"],
    "Existing repository task fixtures validate successfully": ["local-task-contract-tests"],
    "Automated contract tests invoke no model": ["no-model-in-tests"]
  },
  "acceptanceEvidenceRequirements": {},
  "requiredTests": [
    ".agent-workflow/scripts/verify_controller.sh",
    "python3 .agent-workflow/scripts/check_repository.py"
  ],
  "allowedPaths": [
    ".agent-workflow/controller/td_controller/task_contract.py",
    ".agent-workflow/controller/tests/test_task_contract.py",
    ".agent-workflow/tasks/ORCH-001.json"
  ],
  "protectedPaths": [
    ".github/**",
    ".agent-workflow/policy/**",
    ".agent-workflow/scripts/**"
  ],
  "riskClass": "R3",
  "maxChangedLines": 900,
  "maxAttempts": 2,
  "humanApprovalRequired": true
}

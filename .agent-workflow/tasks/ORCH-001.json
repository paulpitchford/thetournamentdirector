{
  "id": "ORCH-003D1A0A1",
  "status": "APPROVED",
  "parentEpic": "ORCHESTRATION-PILOT",
  "objective": "Enforce one-to-one logical ownership of trusted pre-provisioned workspace inodes",
  "nonGoals": ["Create workspace directories", "Export owned descriptors", "Use context-manager cleanup", "Delete or rename workspace directories", "Run Git commands"],
  "dependsOn": ["ORCH-003C"],
  "acceptanceCriteria": [
    "physical-exclusivity|WHEN one inode is registered for another task or attempt|ASSERT TEST_PASS workspace-ownership-tests",
    "path-independent|WHEN an owned pathname is replaced|ASSERT TEST_PASS workspace-ownership-tests",
    "synchronous-cleanup|WHEN concurrent cleanup fails|ASSERT TEST_PASS workspace-ownership-tests",
    "no-path-access|WHEN ownership operations run|ASSERT TEST_PASS workspace-ownership-tests"
  ],
  "acceptanceEvidenceIds": {
    "physical-exclusivity|WHEN one inode is registered for another task or attempt|ASSERT TEST_PASS workspace-ownership-tests": ["workspace-ownership-tests"],
    "path-independent|WHEN an owned pathname is replaced|ASSERT TEST_PASS workspace-ownership-tests": ["workspace-ownership-tests"],
    "synchronous-cleanup|WHEN concurrent cleanup fails|ASSERT TEST_PASS workspace-ownership-tests": ["workspace-ownership-tests"],
    "no-path-access|WHEN ownership operations run|ASSERT TEST_PASS workspace-ownership-tests": ["workspace-ownership-tests"]
  },
  "acceptanceEvidenceRequirements": {},
  "requiredTests": [".agent-workflow/scripts/verify_controller.sh", "python3 .agent-workflow/scripts/check_repository.py"],
  "allowedPaths": [
    ".agent-workflow/controller/td_controller/workspace_ownership.py",
    ".agent-workflow/controller/tests/test_workspace_ownership.py",
    ".agent-workflow/tasks/ORCH-001.json"
  ],
  "protectedPaths": [".github/**"],
  "riskClass": "R3",
  "maxChangedLines": 400,
  "maxAttempts": 2,
  "humanApprovalRequired": true
}

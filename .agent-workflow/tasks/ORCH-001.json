{
  "id": "ORCH-003D1B0O2",
  "status": "APPROVED",
  "parentEpic": "ORCHESTRATION-PILOT",
  "objective": "Build an exact Git blob command and cryptographically verify bounded payload bytes against the tree manifest",
  "nonGoals": ["Write workspace files", "Read denied recovered content", "Batch multiple blobs", "Commit or push"],
  "dependsOn": ["ORCH-003D1B0O1"],
  "acceptanceCriteria": [
    "exact-blob-command|WHEN a materializable entry is supplied|ASSERT TEST_PASS exact-git-blob-tests",
    "digest-binding|WHEN exact payload bytes are verified|ASSERT VALUE \"manifest blob SHA\"",
    "wrong-payload|WHEN payload digest differs|ASSERT ERROR ExactGitBlobError",
    "denied-content|WHEN non-materializable entry is supplied|ASSERT ERROR ExactGitBlobError",
    "binary-roundtrip|WHEN real binary Git blob is read|ASSERT TEST_PASS exact-git-blob-real-proof"
  ],
  "acceptanceEvidenceIds": {
    "exact-blob-command|WHEN a materializable entry is supplied|ASSERT TEST_PASS exact-git-blob-tests": ["exact-git-blob-tests"],
    "digest-binding|WHEN exact payload bytes are verified|ASSERT VALUE \"manifest blob SHA\"": ["exact-git-blob-tests"],
    "wrong-payload|WHEN payload digest differs|ASSERT ERROR ExactGitBlobError": ["exact-git-blob-tests"],
    "denied-content|WHEN non-materializable entry is supplied|ASSERT ERROR ExactGitBlobError": ["exact-git-blob-tests"],
    "binary-roundtrip|WHEN real binary Git blob is read|ASSERT TEST_PASS exact-git-blob-real-proof": ["exact-git-blob-real-proof"]
  },
  "acceptanceEvidenceRequirements": {},
  "requiredTests": [".agent-workflow/scripts/verify_controller.sh", "python3 .agent-workflow/scripts/check_repository.py"],
  "allowedPaths": [
    ".agent-workflow/controller/td_controller/exact_git_blob.py",
    ".agent-workflow/controller/tests/test_exact_git_blob.py",
    ".agent-workflow/tasks/ORCH-001.json"
  ],
  "protectedPaths": [".github/**"],
  "riskClass": "R3",
  "maxChangedLines": 300,
  "maxAttempts": 2,
  "humanApprovalRequired": true
}

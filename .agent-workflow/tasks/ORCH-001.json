{
  "id": "ORCH-003D1B0O6B",
  "status": "APPROVED",
  "parentEpic": "ORCHESTRATION-PILOT",
  "objective": "Lazily enumerate bounded filesystem names with unconditional iterator cleanup",
  "nonGoals": ["Inspect entry metadata", "Traverse child directories", "Modify filesystem state"],
  "dependsOn": ["ORCH-003D1B0O5"],
  "acceptanceCriteria": [
    "real-directory|WHEN bounded directory is enumerated|ASSERT TEST_PASS bounded-directory-entry-tests",
    "lazy-bound|WHEN iterator exceeds limit|ASSERT VALUE \"consume only first excess entry\"",
    "cleanup|WHEN validation or enumeration fails|ASSERT VALUE \"iterator closed in finally\"",
    "filesystem-bytes|WHEN valid name contains non-UTF8 byte|ASSERT VALUE \"os.fsencode round trip\"",
    "unicode-failure|WHEN synthetic name cannot be filesystem-encoded|ASSERT ERROR BoundedDirectoryEntriesError"
  ],
  "acceptanceEvidenceIds": {
    "real-directory|WHEN bounded directory is enumerated|ASSERT TEST_PASS bounded-directory-entry-tests": ["bounded-directory-entry-tests"],
    "lazy-bound|WHEN iterator exceeds limit|ASSERT VALUE \"consume only first excess entry\"": ["bounded-directory-entry-tests"],
    "cleanup|WHEN validation or enumeration fails|ASSERT VALUE \"iterator closed in finally\"": ["bounded-directory-entry-tests"],
    "filesystem-bytes|WHEN valid name contains non-UTF8 byte|ASSERT VALUE \"os.fsencode round trip\"": ["bounded-directory-entry-tests"],
    "unicode-failure|WHEN synthetic name cannot be filesystem-encoded|ASSERT ERROR BoundedDirectoryEntriesError": ["bounded-directory-entry-tests"]
  },
  "acceptanceEvidenceRequirements": {},
  "requiredTests": [".agent-workflow/scripts/verify_controller.sh", "python3 .agent-workflow/scripts/check_repository.py"],
  "allowedPaths": [
    ".agent-workflow/controller/td_controller/bounded_directory_entries.py",
    ".agent-workflow/controller/tests/test_bounded_directory_entries.py",
    ".agent-workflow/tasks/ORCH-001.json"
  ],
  "protectedPaths": [".github/**"],
  "riskClass": "R3",
  "maxChangedLines": 300,
  "maxAttempts": 2,
  "humanApprovalRequired": true
}

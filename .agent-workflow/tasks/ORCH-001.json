{
  "id": "ORCH-003D1B0K",
  "status": "APPROVED",
  "parentEpic": "ORCHESTRATION-PILOT",
  "objective": "Atomically reserve a controller-derived Git branch at the exact approved base commit",
  "nonGoals": ["Defend against code already running as the trusted controller UID", "Create a worktree", "Checkout a branch", "Materialize files", "Commit changes"],
  "dependsOn": ["ORCH-003D1B0J2", "ORCH-003A"],
  "acceptanceCriteria": [
    "atomic-ref|WHEN an absent derived branch is reserved|ASSERT TEST_PASS git-reservation-proof",
    "duplicate-ref|WHEN the same generation is reserved twice|ASSERT ERROR GitReservationError",
    "exact-base|WHEN the approved commit is absent|ASSERT ERROR GitReservationError",
    "live-authority|WHEN the workspace handle is closed|ASSERT ERROR GitReservationError"
  ],
  "acceptanceEvidenceIds": {
    "atomic-ref|WHEN an absent derived branch is reserved|ASSERT TEST_PASS git-reservation-proof": ["git-reservation-proof"],
    "duplicate-ref|WHEN the same generation is reserved twice|ASSERT ERROR GitReservationError": ["git-reservation-tests"],
    "exact-base|WHEN the approved commit is absent|ASSERT ERROR GitReservationError": ["git-reservation-tests"],
    "live-authority|WHEN the workspace handle is closed|ASSERT ERROR GitReservationError": ["git-reservation-tests"]
  },
  "acceptanceEvidenceRequirements": {},
  "requiredTests": [".agent-workflow/scripts/verify_controller.sh", "python3 .agent-workflow/scripts/check_repository.py"],
  "allowedPaths": [
    ".agent-workflow/controller/td_controller/git_reservation.py",
    ".agent-workflow/controller/tests/test_git_reservation.py",
    ".agent-workflow/tasks/ORCH-001.json"
  ],
  "protectedPaths": [".github/**"],
  "riskClass": "R3",
  "maxChangedLines": 400,
  "maxAttempts": 2,
  "humanApprovalRequired": true
}

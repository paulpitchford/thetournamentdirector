{
  "id": "ORCH-003D1B0O3A",
  "status": "APPROVED",
  "parentEpic": "ORCHESTRATION-PILOT",
  "objective": "Publish one verified workspace blob without destructive cleanup after publication uncertainty",
  "nonGoals": ["Materialize a full tree", "Automatically reconcile post-publication races", "Commit or push"],
  "dependsOn": ["ORCH-003D1B0N5E", "ORCH-003D1B0O2"],
  "acceptanceCriteria": [
    "atomic-blob|WHEN exact verified blob is written|ASSERT TEST_PASS workspace-blob-writer-tests",
    "pinned-path|WHEN workspace or parent path is replaced|ASSERT ERROR WorkspaceBlobIndeterminateError",
    "replacement-preserved|WHEN published target is replaced concurrently|ASSERT VALUE \"replacement remains intact\"",
    "durable-mode|WHEN final mode is applied|ASSERT VALUE \"file synced before publish\"",
    "no-overwrite|WHEN target already exists|ASSERT ERROR WorkspaceBlobRejectedError",
    "in-place-race|WHEN published content mutates during resolution|ASSERT ERROR WorkspaceBlobIndeterminateError"
  ],
  "acceptanceEvidenceIds": {
    "atomic-blob|WHEN exact verified blob is written|ASSERT TEST_PASS workspace-blob-writer-tests": ["workspace-blob-writer-tests"],
    "pinned-path|WHEN workspace or parent path is replaced|ASSERT ERROR WorkspaceBlobIndeterminateError": ["workspace-blob-writer-tests"],
    "replacement-preserved|WHEN published target is replaced concurrently|ASSERT VALUE \"replacement remains intact\"": ["workspace-blob-writer-tests"],
    "durable-mode|WHEN final mode is applied|ASSERT VALUE \"file synced before publish\"": ["workspace-blob-writer-tests"],
    "no-overwrite|WHEN target already exists|ASSERT ERROR WorkspaceBlobRejectedError": ["workspace-blob-writer-tests"],
    "in-place-race|WHEN published content mutates during resolution|ASSERT ERROR WorkspaceBlobIndeterminateError": ["workspace-blob-writer-tests"]
  },
  "acceptanceEvidenceRequirements": {},
  "requiredTests": [".agent-workflow/scripts/verify_controller.sh", "python3 .agent-workflow/scripts/check_repository.py"],
  "allowedPaths": [
    ".agent-workflow/controller/td_controller/workspace_blob_writer.py",
    ".agent-workflow/controller/tests/test_workspace_blob_writer.py",
    ".agent-workflow/tasks/ORCH-001.json"
  ],
  "protectedPaths": [".github/**"],
  "riskClass": "R3",
  "maxChangedLines": 450,
  "maxAttempts": 2,
  "humanApprovalRequired": true
}
